import React from 'react';
import UserCard from '../components/user-card';

const Matching = () => {
    return (
        <section>
            <UserCard/>
        </section>
    );
};

export default Matching;